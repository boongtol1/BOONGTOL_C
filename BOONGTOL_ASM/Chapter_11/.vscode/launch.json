{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Compiled Assembly",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build Assembly",
      "console": "integratedTerminal"
    }
  ]
}
